
#include <stdio.h>
#include <math.h>

float quadratic (int a, int b, int c)
{
    float x = 0;
    x = sqrt(2);
    return x;
    
}

int main()
{
    float y = quadratic (1,2,3);
    printf("result: %.2f", y);
    return 0;
}
